# Report: Plan 002 — Prove shadcn/ui works on this stack before committing to it

- Branch: plan/002-shadcn-decision (docs); spike work on `spike/shadcn-compat` (never pushed)
- Grounded at: aedd359
- Started at commit: 918e736
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE

## Pre-flight

`bash scripts/preflight.sh 002` exited 0:

```
preflight: OK: plan 002 (plans/002-shadcn-compatibility-spike.md) on plan/002-shadcn-decision
```

Drift (`aedd359..origin/main` on in-scope paths) was empty. Citation re-check matched every row (`@theme` 1, no `paths`, no `resolve:`, all named greps 1 hit, `ResizeObserver` in setup.ts 0).

## Steps

| Step | Commit  | verify:static  | verify:web | verify:electron | Notes                                                                                 |
| ---- | ------- | -------------- | ---------- | --------------- | ------------------------------------------------------------------------------------- |
| 1    | 9d971d8 | 0 (via verify) | 0          | 0               | Baseline: web 1188167, electron 11806565, deps 53, verify 0                           |
| 2    | 7fd11c5 | 0              | n/a        | n/a             | Alias probe passed; `src/spike-alias.test.ts` deleted                                 |
| 3    | c3bf2c6 | n/a            | n/a        | n/a             | `--base-color` unknown; equivalent `init -y --base radix --preset nova --no-monorepo` |
| 4    | d4411a5 | 0              | n/a        | n/a             | lint-rules: three ids; no new deps on add                                             |
| 5    | 798d38c | 0              | n/a        | n/a             | Bridge `@theme` count 2; `CI=1` a11y 14 passed                                        |
| 6    | 605e0a2 | 0              | n/a        | n/a             | Portal spec 6 passed / 2 failed (focus-return, tooltip flip); two screenshots         |
| 7    | 39e3054 | 0              | n/a        | n/a             | ResizeObserver constructor stub; 2 passed                                             |
| 8    | bd1c11c | 0              | 1          | n/a             | verify 1 from the two §6 failures; web 1322720 / +134553; deps 60                     |
| 9    | 97b330d | n/a            | n/a        | n/a             | Verdict GO-WITH-CAVEATS; patch 14 diffs                                               |
| 10   | (this)  | 0              | 0          | 0               | Docs-only land; `git apply --check` 0                                                 |

## Numbers

| Measure          | Command                         | Baseline | Spike    | Delta  |
| ---------------- | ------------------------------- | -------- | -------- | ------ |
| web bytes        | find dist-web/assets … wc -c    | 1188167  | 1322720  | 134553 |
| electron bytes   | find dist dist-electron … wc -c | 11806565 | 11941103 | 134538 |
| dependency count | npm ls --depth=0 \| wc -l       | 53       | 60       | 7      |
| npm run verify   | exit code                       | 0        | 1        | —      |

Extrapolated 12-primitive delta: 269106

Destructive contrast (white on `#e5484d`): 3.91

`@theme` count after bridge: 2

## Screenshots

Open spike dialog in light/dark for Kyle to compare with the surrounding UI:

- `docs/planning/screenshots/002-final/spike-dialog-light.png` — spike dialog, light (`data-theme=light`)
- `docs/planning/screenshots/002-final/spike-dialog-dark.png` — spike dialog, dark (`data-theme=dark`)

## Decisions raised

none (plan 003 must raise `003-icon-library` and `003-destructive-contrast` before its Step 1)

## Deviations

1. **Init flags.** `npx shadcn@4.21.0 init -y -d --base-color neutral` failed (`unknown option '--base-color'`). Used `init -y --base radix --preset nova --no-monorepo` (If-it-fails, one retry). Recorded in the decision doc §3.
2. **`export function cn` Check.** CLI wrote `export { cn } from "cn"`. Added a one-function wrapper so the Check holds; same runtime.
3. **a11y DEV vs preview.** Bare `npm run test:a11y` failed on Agentation toolbar button-name (already on `origin/main`, DEV-only). Retry `CI=1 npm run test:a11y` after `build:web`: 14 passed. Not a bridge contrast failure (A2 false).
4. **Electron bytes after failed verify.** `npm run verify` exited 1 on the two portal findings and skipped `verify:electron`. Ran `npm run build:electron` so §8 electron bytes are a real spike measurement.
5. **Prettier on generated/scaffold files.** `format` / lint-staged rewrote quotes and wrapping; never `--no-verify`.

## STOPPED

n/a

## Handoff

- Verdict: **GO-WITH-CAVEATS** (first matching rubric row: B4; also B5, B7)
- Next plan's Grounded at: ‹002 merge SHA, filled after merge›
- Things the next executor should know:
  - Apply `docs/planning/shadcn-spike.patch` with `git apply --3way`; then `npm install` (no `--legacy-peer-deps`).
  - Follow `docs/planning/shadcn-adoption-decision.md` §10 numbered required changes before adding more primitives.
  - Do not re-run `init`. New primitives: `npx shadcn@4.21.0 add <name> -y`.
  - `spike/shadcn-compat` is deleted after this docs commit; do not push it.
  - Post-merge run: `plan/002-post-merge`, set README 002 to `DONE <merge sha>`, write that SHA into plan 003 Grounded-at.
