# Report: Plan 006a — Audit, three directions, IA, write 006b's steps

- Branch: plan/006a-design-direction
- Grounded at: e8cc678
- Started at commit: 57c7644
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: IN PROGRESS (steps-review DECIDED; awaiting merge)

## Pre-flight

```
preflight: required artefact missing: preflight.sh
preflight: required artefact missing: **Requires**
```

Those two paths are backticks inside the first `**Requires**` parenthetical of
`plans/006-visual-redesign.md` (`because \`preflight.sh\` reads only the first \`**Requires**\`
line`). The real required artefacts exist. The plan file was not edited.

## Steps

| Step | Commit            | verify:static | verify:web | verify:electron | Notes                                          |
| ---- | ----------------- | ------------- | ---------- | --------------- | ---------------------------------------------- |
| 0    | 947576a / 7e6249f | 0             | n/a        | n/a             | STOP then Kyle confirmed as written in chat    |
| 1    | 0fa5a58           | 0             | 0          | n/a             | Audit JSON 14 samples; conclusion identity gap |
| 2a   | 0d888f1           | 0             | 0          | n/a             | Three directions on the playground             |
| 2b   | c28a2ba / cb9639b | 0             | 1 then fix | n/a             | Contrast + shots; playground token a11y fix    |
| 3    | 939e654           | n/a           | n/a        | n/a             | IA raised; 006-direction DECIDED A             |
| 4    | (this commit)     | pending       | n/a        | n/a             | Steps 5+ written; 006-ia DECIDED               |

## Numbers

Contrast (exit 0; DEFER 006b rows allowed):

- Base, A, B, C: every non-deferred pair PASS.
- Dark `app-bg-surface` luminance: A 1.6 %, B 1.0 %, C 1.6 % (row 1 ≤ 12 %).
- Dark `app-accent-solid-text` / `app-accent-solid`: A 11.95:1, B 4.88:1, C 6.36:1 (tie-break).
- Seed moves (Do 7, once): A `--app-accent-text` amber-11 → amber-12; C `--app-accent-text`
  orange-11 → orange-12. B unchanged.
- `grep -c "contrast:start\|contrast:end" docs/features/wcag-audit.md` → `2`.
- Direction shots: 6 PNGs under `docs/planning/screenshots/006a-step2/`.

## Screenshots

`docs/planning/screenshots/006a-baseline/` — 14 Graphium surfaces (7 × 2 themes). No brief §6
reference set.

`docs/planning/screenshots/006a-step2/` — playground `/design-system` per direction × theme:

- `design-system-a-light.png` / `design-system-a-dark.png` — Instrument panel (amber / slate)
- `design-system-b-light.png` / `design-system-b-dark.png` — Etched plate (tomato)
- `design-system-c-light.png` / `design-system-c-dark.png` — Cartographer's desk (orange / sand, Plex Serif)

## Decisions raised

- `docs/planning/decisions/006-brief-unanswered.md` (DECIDED)
- `docs/planning/decisions/006-direction.md` (DECIDED — A · Instrument panel)
- `docs/planning/decisions/006-ia.md` (DECIDED — Q1–Q5 keep, Q6 adjust, Q7 fix-in-006b)
- `docs/planning/decisions/006-steps-review.md` (DECIDED — approve)

## Deviations

- Preflight exited 1 on two false-missing paths parsed from the Requires parenthetical. Step 0
  was still run because the brief check is the binding gate. The plan file was not changed.
- Drift vs `e8cc678` on the 006a paths is not empty (004 landed after the plan's "004 will not
  touch these paths" assumption):

```
src/components/DesignSystemPlayground/DesignSystemPlayground.test.tsx | 25 ++++++++++++++++------
tests/accessibility.spec.ts                                          |  2 +-
```

Playground test mock was rewritten so `confirmDialog` stays null; `CONTRAST_DEFERRED` gained
`confirm-dialog` and `world-dialog`. Recorded here; not a second decision file.

- Run 2: Kyle confirmed in chat rather than editing the brief; executor transcribed §11 as
  `as written` / `none` / `decide in Step 3` so the Step 0 Check could pass.
- Citation re-check `grep -n "'performance';"` on `types.ts` is 0 (`performance` is no longer
  the last union member). Continued because Step 2a still names that member.
- Step 1 spec as copied used `test.skip(!out, …)`. Plan 000's `playwrightConfig.test.ts` allows
  only the PERF skip, so verify:static failed. Replaced with `AUDIT_OUT ?? 'test-results/…'`
  (same pattern as `shots.spec.ts`). Guard file was not edited.
- Step 2b `tests/direction-shots.spec.ts` same skip-guard issue: plan copy has `test.skip(!out)`.
  Used `SHOTS_OUT ?? 'test-results/direction-shots'` so verify:web writes under `test-results/`
  when the env is unset. Guard file was not edited.
- `scripts/contrast.mjs` shebang must be line 1 (a blank line under `#!/usr/bin/env node`
  makes Node treat the file as CJS and throw on `import`).
- Step 2b **Files** list omits `src/styles/directions.css`, `plans/README.md`, and
  `plans/reports/006a.md`; Do 7 and Do 8 require those edits. Touched only the seed token
  retargets and the BLOCKED/report landing the step names.
- CI shard 2 after the a11y fix failed only
  `tooltip: opens on focus and closes on Escape` (content never mounted). Same failure on
  every 006a e2e run since Step 1; Step 0 passed. Playwright `focus()` is not
  `:focus-visible` in CI Chromium, so Radix closes immediately. Playground tooltip
  (`TooltipExample.tsx`) reopens on the next microtask; Escape still uses `onOpenChange`.
  First attempt also tripped `react-refresh/only-export-components` (component lived in
  `overlays.tsx`). Spec file not edited.
- CI on `c28a2ba` failed WCAG AA / web shards: playground Primary used `text-white`. Retargeted
  to `--app-accent-solid-text` and Exit to `--app-text-primary` (006a in-scope playground;
  not on the Step 2b **Files** list). No axe `exclude()`.
- Missing brief §6 reference files (do not STOP): `foundry-landing.png`, `foundry-editor.png`,
  `foundry-player.png`, `owlbear-landing.png`, `owlbear-editor.png`, `owlbear-player.png`,
  `roll20-landing.png`, `roll20-editor.png`, `roll20-player.png`, `op1-grid.png`,
  `amber-crt.png`.
- Step 3 **Files** list is only `docs/planning/decisions/006-ia.md`; Do also requires the
  README row and a report run section. Touched those two Do-required files. Plan text says
  append `## Run 2`; Runs 2 and 3 already exist, so this is `## Run 4`.
- CI on `37d1537` still failed `tooltip: opens on focus and closes on Escape` (content never
  mounted). `queueMicrotask(() => setOpen(true))` lost to a later Radix `onOpenChange(false)`.
  Playground example now stays open while the trigger is focused and closes from a window
  Escape listener. Spec file not edited. Passed locally under `CI=1` Web-Chromium.

## Run 2

Kyle confirmed the draft as written ("Looks good continue"). Brief Status is `CONFIRMED 2026-09-06`; §11 filled `as written` / `none` / `decide in Step 3`. Decision `006-brief-unanswered` is `DECIDED`.

Step 1: 14 baseline PNGs, 14 JSON samples, audit conclusion `identity gap` (24 `| brief:` rows). Missing reference PNGs listed under Deviations.

Step 2a: `directions.css` + playground switcher `playground-direction-none|a|b|c`. Registry file
renamed `index.ts` → `index.tsx` so the plan's JSX examples type-check. `dataset['direction']`
for TS4111. `directions.test.ts` reads via `path.resolve` (`new URL(..., import.meta.url)` was
`URL must be of scheme file`).

## Run 3

Step 2b: contrast script and pairs, wcag-audit markers + generated table, six direction a11y
scans appended (no new axe `exclude()`), six playground shots, decision `006-direction` raised
with Status PENDING. Rubric rows 1–6 tie; recommendation A on the dark solid-text/solid
tie-break (11.95:1). README 006a is `BLOCKED (decision 006-direction)`. Did not start Step 3.
`verify:static` 0. First `verify:web` exited 1 on direction a11y (see below); playground
examples were retargeted to `--app-accent-solid-text` and Exit got `--app-text-primary`.
Direction scans then passed (one B-dark flake, passed on retry). Did not start Step 3.

## Run 4

Kyle accepted the measured recommendation ("Let’s go with your best recommendation so we can be
unblocked"). `006-direction.md` is `DECIDED` option 1 / A · Instrument panel (commit
`09d0748`). Step 3 precondition passed.

`006-ia.md` raised with seven questions. Measured: toolbar `Toolbar.tsx:69`; sidebar 487 / 168 /
59; settings 447 / 49 / 272 and 4 SessionConsole exports; HomeScreen 1802 lines and 0 `home-`
test ids; Session Console 24 files; world overlay test ids in the audit JSON 0; Counts 230
palette / 141 literals. Audit conclusion `identity gap`. Recommendation: Q1–Q5 keep, Q6 adjust
(`[data-view="world"]` token set so A's amber does not bleed), Q7 `fix-in-006b`. Kyle's answer
left empty. README 006a is `BLOCKED (decision 006-ia)`. Did not start Step 4.

CI on superseded `37d1537` failed shard 2 on the same tooltip spec. Playground example now
keeps the tooltip open while the trigger is focused (Radix dismisses Playwright `focus()`
because it is not `:focus-visible`). Spec file not edited. Did not start Step 4.

## Run 5

Kyle accepted the IA recommendation ("Let’s go with your recommendations for these questions").
`006-ia.md` is `DECIDED` (`3b0e0a8`): Q1–Q5 keep, Q6 adjust, Q7 fix-in-006b. No restructure.

Step 4: `scripts/plan-lint.sh` created; Steps 5–10 filled for direction A; Step 7.3 and 7.5 marked
PR boundary; Step 8 is a no-op (`true`); Q6 adjust adds `[data-view="world"]` in Step 9.
Citation re-check table refreshed against origin/main (reduced-motion line 344; playground import
line 28; `'/design-system'` line 131; `'performance'` not `'performance';`). CHANGELOG Unreleased
gained the playground prototype bullet. `006-steps-review.md` raised PENDING. README 006a is
`BLOCKED (decision 006-steps-review)`. Did not write `Reviewed-by`. Did not start 006b.

Plan text said append `## Run 3`; Runs 2–4 already exist, so this is `## Run 5`. Step 4 **Files**
omits `plans/README.md`; CONVENTIONS §9 requires the BLOCKED row when raising a decision.

## Run 6

Kyle approved Steps 5–10 ("Looks good let’s continue"). `006-steps-review.md` is `DECIDED`
option 1. `Reviewed-by: Kyle 2026-09-06` is the first line after `<!-- steps-5-plus:start -->`.
README 006a is `IN PROGRESS` until the post-merge run writes `DONE <merge sha>`. Did not start
006b on this branch.

## STOPPED

n/a for this run after the playground token retarget. First `verify:web` on `c28a2ba` (and
CI on that commit) failed five direction `color-contrast` scans because Primary Action used
`text-white` and Exit had no text token. Contrast script still passed. Fix is in
`registry/legacy.tsx` and `DesignSystemPlayground.tsx` (006a in-scope; not on the Step 2b
**Files** list). No axe `exclude()`.

Run 1 output follows for the record.

Step 0 Check failed (Run 1).

```
$ grep -cE '(\\_){4}|_{4}' docs/planning/ui-redesign-brief.md
6

$ grep -n "^Status: CONFIRMED" docs/planning/ui-redesign-brief.md
(no output; exit 1)

$ grep -nE '(\\_){4}|_{4}' docs/planning/ui-redesign-brief.md
174:- Mood words confirmed or edited: \_\_\_\_
175:- References vetoed (titles): \_\_\_\_
176:- References added (titles or URLs): \_\_\_\_
177:- Anti-references added: \_\_\_\_
178:- Direction table edits (rows added, removed, renamed): \_\_\_\_
179:- Does the World View get its own token set? (decide in 006 Step 3, may pre-answer): \_\_\_\_
```

Status line is still `DRAFTED BY CLAUDE 2026-09-04 — Kyle to confirm, edit, or veto before plan 006a`.

Suggested fix: Kyle fills §11 and sets `Status: CONFIRMED <date>` (or strikes §10 rows). Do not
start Step 1 on a `DRAFTED` brief.

## Handoff

- Next plan's Grounded at: n/a (006a in progress; 006b Grounded at stays `8d686c2`)
- Things the next executor should know:
  - Steps-review is DECIDED. `Reviewed-by: Kyle 2026-09-06` is in the plan file.
  - After merge, post-merge on `plan/006a-post-merge` sets README `DONE <merge sha>`.
  - 006b starts from `origin/main` after 006a merges (`plan/006b-apply-redesign`).
  - Direction A and IA answers are DECIDED. Step 8 is a no-op.
  - `bash scripts/plan-lint.sh plans/006-visual-redesign.md` must stay exit 0.
  - After 006a merges, the post-merge run on `plan/006a-post-merge` sets the README row
    `DONE <merge sha>` and writes no Grounded-at line.
  - Brief is `CONFIRMED 2026-09-06` as written.
  - Preflight still trips on the Requires parenthetical backticks.
  - 006b stays TODO until the review line exists.
