# Report: Plan 000 — Verification infrastructure

- Branch: plan/000-verification-infrastructure
- Grounded at: d3d3642
- Started at commit: a1eb655 (origin/main at start; branch also carries the plan-set history below the merge)
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE

## Pre-flight

Plan 000 predates `scripts/preflight.sh`. Manual checks from CONVENTIONS §3:

```
npm install                         # exit 0 (node_modules present)
npx playwright install chromium     # exit 0
git branch --show-current           # plan/000-verification-infrastructure
```

Drift check (`git diff --stat d3d3642..origin/main` on the plan's in-scope paths) was empty after merging `origin/main` (`a1eb655`). Citation re-check at start matched every row.

`scripts/preflight.sh` did not exist until Step 1; it was not run before Step 1.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                                                                           |
| ---- | ------- | ------------- | ---------- | --------------- | ----------------------------------------------------------------------------------------------- |
| 1    | 6d9858d | 0             | n/a        | n/a             | Baseline doc, `verify:*` scripts, `scripts/preflight.sh`                                        |
| 2    | 6024f9f | 0             | 0          | n/a             | Surface helper, shots harness, toolbar test ids. Mobile New Campaign uses DOM click (deviation) |
| 3    | 2195bfb | 0             | 0          | n/a             | `tests/visual.spec.ts` + 14 committed baselines                                                 |
| 4    | 75d7a26 | 0             | 0          | n/a             | 14-scan a11y; `role="img"` on session-console-status; `CONTRAST_DEFERRED = ['editor-mobile']`   |
| 5    | abbe64b | 0             | 0          | n/a             | 12 overlay root test ids                                                                        |
| 6    | d35c82e | 0             | 0          | n/a             | Overlay contract: 8 exercised, 5 unreachable, no `trapsFocus` flips                             |
| 7    | a40f5bb | 0             | 0          | n/a             | Deleted 10 stale specs; 5 failing `dm-world-sync` tests; `testIgnore` is three patterns         |
| 8    | c411538 | 0             | 0          | n/a             | Touch-target spec; desktop `.btn-tool` pinned at 46 × 30                                        |
| 9    | 294e378 | 0             | 0          | n/a             | Non-colour token families + one `@theme inline`; visual baselines unchanged                     |
| 10   | a78fc4f | 0             | 0          | n/a             | Deleted `PreferencesDialog`; un-exported unused types (deviation); pinned `deploy-web`          |
| 11   | 6188ebb | 0             | 0          | n/a             | Testing docs match the repo                                                                     |
| 12   | (this)  | 0             | 0          | 0               | Baseline complete (12 `##`); `npm run verify` exit 0; 14 final shots                            |

`npm run verify` at Step 12: `verify:static` 0 (1131 tests / 79 files), `verify:web` 0 (73 passed + 14 a11y), `verify:electron` 0 (7 passed). No Electron failure to defer.

## Numbers

| Metric                      | Command / source                                                                                                                                                                           | Before (`d3d3642`)           | After                         |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------- | ----------------------------- |
| Palette-class hits          | `grep -rnoE '\b(bg\|text\|border\|ring)-(white\|black\|slate\|gray\|zinc\|neutral\|blue\|red\|green\|amber\|orange\|yellow\|purple\|indigo)(-[0-9]{2,3})?\b' src --include=*.tsx \| wc -l` | 400                          | 396                           |
| Inline `style={{`           | `grep -rn "style={{" --include=*.tsx src \| wc -l`                                                                                                                                         | 286                          | 241                           |
| Unique `data-testid`        | `grep -rhoE 'data-testid="[^"]+"' src/ \| sort -u \| wc -l`                                                                                                                                | 22                           | 47                            |
| Web-Chromium list           | `CI=1 npx playwright test --project=Web-Chromium --list \| tail -1`                                                                                                                        | `Total: 13 tests in 2 files` | `Total: 73 tests in 10 files` |
| Electron-App list           | `CI=1 npx playwright test --project=Electron-App --list \| tail -1`                                                                                                                        | `Total: 7 tests in 2 files`  | `Total: 7 tests in 2 files`   |
| `@theme` in `src/index.css` | `grep -c '@theme' src/index.css`                                                                                                                                                           | 0                            | 1                             |
| `push:` in deploy-web       | `grep -cE '^\s*push:' .github/workflows/deploy-web.yml`                                                                                                                                    | 1                            | 0                             |
| Baseline `##` count         | `grep -cE '^## ' docs/planning/verification-baseline.md`                                                                                                                                   | n/a                          | 12                            |
| Final shots                 | `ls docs/planning/screenshots/000-final/*.png \| wc -l`                                                                                                                                    | n/a                          | 14                            |
| Vitest (`test:run`)         | `npm run test:run`                                                                                                                                                                         | 1129 / 78 files              | 1131 / 79 files               |

## Screenshots

Closing set from `SHOTS_OUT=docs/planning/screenshots/000-final npm run shots` (14 passed). No colour or rendered-output change was in scope; these match the Step 3 visual baselines. Kyle should confirm they look like the current product, not a redesign.

- `docs/planning/screenshots/000-final/home-light.png` — home, light; expected unchanged
- `docs/planning/screenshots/000-final/home-dark.png` — home, dark; expected unchanged
- `docs/planning/screenshots/000-final/editor-light.png` — editor, light; expected unchanged
- `docs/planning/screenshots/000-final/editor-dark.png` — editor, dark; expected unchanged
- `docs/planning/screenshots/000-final/editor-mobile-light.png` — editor-mobile, light; expected unchanged
- `docs/planning/screenshots/000-final/editor-mobile-dark.png` — editor-mobile, dark; expected unchanged
- `docs/planning/screenshots/000-final/confirm-dialog-light.png` — confirm-dialog, light; expected unchanged
- `docs/planning/screenshots/000-final/confirm-dialog-dark.png` — confirm-dialog, dark; expected unchanged
- `docs/planning/screenshots/000-final/world-light.png` — world, light; expected unchanged
- `docs/planning/screenshots/000-final/world-dark.png` — world, dark; expected unchanged
- `docs/planning/screenshots/000-final/world-dialog-light.png` — world-dialog, light; expected unchanged
- `docs/planning/screenshots/000-final/world-dialog-dark.png` — world-dialog, dark; expected unchanged
- `docs/planning/screenshots/000-final/design-system-light.png` — design-system, light; expected unchanged
- `docs/planning/screenshots/000-final/design-system-dark.png` — design-system, dark; expected unchanged

## Decisions raised

none

## Deviations

1. **Mobile New Campaign click (Step 2).** `tests/helpers/surfaces.ts` enters the editor with `locator(...).evaluate((el) => el.click())` instead of Playwright's `click()`. On the 390×844 viewport the New Campaign button is below the fold; Playwright's actionability check refused the click even with `force: true` / `scrollIntoViewIfNeeded`. Recorded in a comment on the helper.
2. **Un-exported types in `src/store/touchSettingsStore.ts` (Step 10).** Deleting `PreferencesDialog` left `import/no-unused-modules` on `PressureCurve` and `PalmRejectionMode`. Their `export` keywords were removed. That file was not on Step 10's Files list.
3. **Branch creation.** The branch was created from the plan-set branch and then merged `origin/main` (`057714b`), not `git checkout -b plan/000-verification-infrastructure origin/main`. In-scope drift vs `d3d3642` was empty. Plan-set commits sit below the merge.
4. **Draft PR opened before Step 12 verify.** Cloud-agent rules require a PR before testing. https://github.com/kocheck/Graphium/pull/261 was opened as a draft after Step 11. `npm run verify` later exited 0; the PR is updated with this report.

## STOPPED

n/a

## Handoff

- Next plan's Grounded at: (filled after Kyle merges this PR as a **merge commit**, never squash)
- Things the next executor should know:
  - Post-merge run first: branch `plan/000-post-merge` off `origin/main`, one commit `plan-000 post-merge: record merge sha`, set `plans/README.md` row 000 to `DONE <merge sha>`, write that SHA into `plans/001-stabilize-styling-foundation.md` Grounded-at. Then `grep -c "DONE" plans/README.md` must print `1`.
  - After that, execute plan 001. Sequence: 001 → 002 → 003 → 006a → 004 (six PRs) → 005 → 006b.
  - Plan 006a Step 0 **refuses** until Kyle fills the answers in `docs/planning/ui-redesign-brief.md`.
  - `gotoSurface` on mobile uses a DOM click for New Campaign (deviation 1). Do not "fix" it back to Playwright `click()` without a viewport/layout change.
  - `CONTRAST_DEFERRED` is `['editor-mobile']` (mobile toolbar "Select", 2.89). Plan 006b empties the list; do not add exclusions.
  - Five overlays are unreachable from a fresh campaign: MobileBottomSheet, AddToLibraryDialog, TokenMetadataEditor, ImageCropper, SessionConsoleEditorSheet. Plan 004 flips those rows when it migrates them.
  - `deploy-web.yml` has no `push:` trigger until 006b restores it.
  - Later plans run `bash scripts/preflight.sh NNN` and cite palette-class 396 from `docs/planning/verification-baseline.md`.
