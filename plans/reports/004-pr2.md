# Report: Plan 004 PR 2/6 — No-a11y overlays

- Branch: plan/004-pr2-no-a11y-overlays
- Grounded at: f53ef0a (PR 1)
- Started at commit: 309cb75
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE (PR 2/6; waiting on merge)

## Pre-flight

`bash scripts/preflight.sh 004` exited 0 on `plan/004-pr2-no-a11y-overlays`.

Drift vs `f53ef0a` on in-scope `src/` `tests/` `scripts/` `.eslintrc.cjs` `docs/guides/UI_RECIPES.md` was empty. The plan's exact `Grounded at\*\*:` grep does not match `Grounded at (PR 1)` (the `(PR 1)` sits between `Grounded at` and `**:`); used `f53ef0a` from the Status line.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                                   |
| ---- | ------- | ------------- | ---------- | --------------- | ------------------------------------------------------- |
| 4a   | 8b373aa | 0             | 0          | n/a             | MapSettingsSheet; BASELINE 378 → 377                    |
| 4b   | 93b735c | 0             | 0          | n/a             | AddToLibraryDialog; BASELINE 350                        |
| 4c   | (this)  | 0             | 0          | 0               | ImageCropper + new test; BASELINE 339; Canvas untouched |

## Numbers

### Card rows (before → after)

```
| `src/components/MapSettingsSheet.tsx` | before palette=1 inline=7 btn=6 legacy=3 | after lines=447 ui-imports=3 role=0 aria-modal=0 Escape=0 esc-owns=0 test=no palette=0 inline=0 btn=0 legacy=0 | data-testid="sheet-map-settings-root" |
| `src/components/AssetLibrary/AddToLibraryDialog.tsx` | before palette=27 inline=1 | after lines=296 ui-imports=3 role=0 aria-modal=0 Escape=0 esc-owns=0 test=no palette=0 inline=0 | data-testid="dialog-add-to-library-root" |
| `src/components/ImageCropper.tsx` | before palette=11 inline=0 test=no | after lines=280 ui-imports=2 role=0 aria-modal=0 Escape=0 esc-owns=0 test=yes palette=0 inline=0 | data-testid="dialog-image-cropper-root" |
```

### Ratchet

| After step | BASELINE |
| ---------- | -------- |
| PR 1       | 378      |
| 4a         | 377      |
| 4b         | 350      |
| 4c         | 339      |

## Screenshots

n/a (this PR did not run (S))

## Decisions raised

none

## Deviations

1. **Drift grep vs `(PR k)`**. The Status line is `Grounded at (PR 1)**: \`f53ef0a\``; the plan's `Grounded at\*\*:`pattern does not match it. Drift was run against`f53ef0a` (empty).
2. **MapSettingsSheet grid-type `<select>`**. The plan called both `.sidebar-input` hits "inputs"; line 369 is a `<select>`. Kept as `<select className="w-full">` (not `<Input>`).
3. **ImageCropper cropping** has no automated coverage (jsdom / `react-easy-crop`), as the plan's test file records.

## STOPPED

n/a

## Handoff

- Next run: after this PR merges, `plan/004-pr2-post-merge` records the merge SHA, then branch `plan/004-pr3-sheets-and-library` from `origin/main` (Steps 5a–6b).
- 006a is still blocked on `docs/planning/ui-redesign-brief.md` §11.
- Merge method: merge commit, never squash.
