# Report: Plan 006b — Apply the redesign

- Branch: plan/006b-world-view
- Grounded at: 8d686c2
- Started at commit: b946287
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: STOPPED at Step 9

## Pre-flight

```
preflight: required artefact missing: preflight.sh
preflight: required artefact missing: **Requires**
```

Those two paths are backticks inside the first `**Requires**` parenthetical of
`plans/006-visual-redesign.md`. The real required artefacts exist. The plan file was not edited.

Hand Requires: files-ok; `006-direction.md` and `006-ia.md` both `DECIDED`; `Reviewed-by: Kyle`
count 1.

Drift vs `8d686c2` on 006b paths is not empty — that is 006a landing (directions, playground,
a11y, wcag markers, theme reduced-motion). Recorded; not a second decision file.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                    |
| ---- | ------- | ------------- | ---------- | --------------- | ---------------------------------------- |
| 5    | 9eb36b9 | 0             | 1          | n/a             | Check passed; Commands verify:web failed |
| 5+   | c0b1bb8 | 0             | n/a        | n/a             | Kyle accepted: tokens.spec 8px→4px extra |
| 6    | 6642131 | 0             | n/a        | n/a             | duration tokens; elevation-active press  |
| 7.1  | 63c21c9 | 0             | n/a        | n/a             | toolbar; mobile accent-text              |
| 7.2  | 88c3f26 | 0             | n/a        | n/a             | sidebar hairline / inset active          |
| 7.3  | a329672 | 0             | n/a        | n/a             | home screen A tokens                     |
| 7.4  | 89514b0 | 0             | 1          | n/a             | dialogs; merged #287                     |
| 7.5  | 9b8edd0 | 0             | 1          | n/a             | Session Console; merged #288             |
| 8    | 68cf273 | n/a           | n/a        | n/a             | no-op (`true`); no IA restructure        |
| 9    | 8dce2c4 | n/a           | n/a        | n/a             | spec + world tokens; STOP after retry    |

## Numbers

`npm run contrast` exit 0, no DEFER, no FAIL (Step 5 Check).

- Light `--app-border-default`: `--slate-8` was 1.86:1; `--slate-9` is 3.22:1. Dark `--slate-8` is 3.01:1.
- Light `--app-error-solid`: white and ink failed on `--red-9` and `--red-10`; `--red-11` + white is 5.21:1.
- Light `--app-success-solid-text`: ink (`--slate-12`) on `--green-9` is 5.19:1.
- Accent amber-9 + ink / slate-1: 10.38:1 light / 11.95:1 dark.
- `grep -c '"deferred"' scripts/contrast-pairs.json` → `0`
- `grep -rn "data-direction\|graphium-direction" src tests` → no output
- Radix imports in `theme.css` remain 14.

`npm run verify:web` after 7.4: 77 passed, 9 failed, 6 skipped (known Step 9/10 set).
`npm run verify:web` after 7.5: 76 passed, 9 failed, 1 flaky, 6 skipped (same set).

## Screenshots

- `docs/planning/screenshots/006b-baseline/` — 14 PNGs, captured before applying A.
- `docs/planning/screenshots/006b-step7.1/` through `006b-step7.5/` — 14 PNGs each.

## Decisions raised

none (006-direction A and 006-ia already DECIDED)

## Deviations

- Preflight false-missing Requires backticks (same as 006a).
- Drift vs `8d686c2` is 006a landing.
- `fonts.css` is on the Step 5 Files list but was not edited: A uses Plex Sans and Plex Mono; both faces already live there.
- `.theme-loading * { transition: none !important }` remains after Step 6 (Commands expected `0`; only the reduced-motion `*` rule was deleted).
- `verify:web` accepted red until 9/10 per Kyle 2026-09-06.
- Shots dirs and this report / README treated as do-required extras.
- Step 9 `[data-view="world"]` solid pair is blue-4 / slate (pre-A `#0070c1` / white is 4.51:1, below §8 7:1).
- Step 9 one retry restyled Minimap caption from `theme.css` (Minimap.tsx is not on the Files list).

## STOPPED (cleared 2026-09-06)

Kyle accepted the suggested fix in chat: treat `tokens.spec.ts` (`8px` → `4px`) as a Step 5 extra;
leave visual snapshots to Step 10; leave the two component contrast bugs to Steps 7.1 and 9;
accept `verify:web` red until those steps. Step 5 Check remains the gate.

**Step:** 5 (Check passed; `npm run verify:web` in Commands did not).

## STOPPED

**Step:** 9 (`CI=1 npx playwright test tests/world-legibility.spec.ts --project=Web-Chromium`).

**Condition:** One fix in the permitted files, one retry; then STOP with the failing sample JSON.

**First fail (before retry):**

```
[{"tag":"DIV","text":"Minimap","fontSize":13.5,"strokes":[1],"ratio":1.113417106083212}]
```

Retry in `theme.css`: `--text-xs` floor 18px, `.text-neutral-400` → `--app-text-primary`, `.border` / `.border-t` → 2px. Minimap left the `small` list.

**Second fail (after retry):**

```
[{"tag":"BUTTON","text":"Arm audio","fontSize":18,"strokes":[2,2,2,2],"ratio":1.4818336804016898}]
```

`WorldAudioEngine.tsx` already uses `text-[var(--app-accent-solid-text)]` on `bg-[var(--app-accent-solid)]`. The spec's `lum()` only parses `rgb()` / `rgba()`; it walks to the surface behind the button, so slate-1 (dark-theme `--app-accent-solid-text` on the world block) sits on `--app-bg-surface` at 1.48:1.

**Suggested fix:** Paint "Arm audio" with `text-[var(--app-text-primary)]` (and an opaque `background-color` the sampler can read), and set dark `[data-view="world"] --app-accent-solid-text` to `--slate-12`.

## Handoff

- Branch: `plan/006b-world-view` (PR #289). #287 and #288 are merged.
- 006b Grounded-at stays `8d686c2`.
- Do not start Step 10 until Step 9's Check exits 0.
- Do not re-add `"deferred"` keys or `data-direction`.
- Do not update visual snapshots before Step 10.
- Do not mark 006b `DONE` until Step 10 + post-merge.
