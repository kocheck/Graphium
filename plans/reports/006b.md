# Report: Plan 006b — Apply the redesign

- Branch: plan/006b-apply-redesign
- Grounded at: 8d686c2
- Started at commit: b946287
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: STOPPED at Step 5

## Pre-flight

```
preflight: required artefact missing: preflight.sh
preflight: required artefact missing: **Requires**
```

Those two paths are backticks inside the first `**Requires**` parenthetical of
`plans/006-visual-redesign.md`. The real required artefacts exist. The plan file was not edited.

Hand Requires: files-ok; `006-direction.md` and `006-ia.md` both `DECIDED`; `Reviewed-by: Kyle`
count 1.

Drift vs `8d686c2` on 006b paths is not empty — that is 006a landing (directions, playground,
a11y, wcag markers, theme reduced-motion). Recorded; not a second decision file.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                    |
| ---- | ------- | ------------- | ---------- | --------------- | ---------------------------------------- |
| 5    | 9eb36b9 | 0             | 1          | n/a             | Check passed; Commands verify:web failed |

## Numbers

`npm run contrast` exit 0, no DEFER, no FAIL (Step 5 Check).

- Light `--app-border-default`: `--slate-8` was 1.86:1; `--slate-9` is 3.22:1. Dark `--slate-8` is 3.01:1.
- Light `--app-error-solid`: white and ink failed on `--red-9` and `--red-10`; `--red-11` + white is 5.21:1.
- Light `--app-success-solid-text`: ink (`--slate-12`) on `--green-9` is 5.19:1.
- Accent amber-9 + ink / slate-1: 10.38:1 light / 11.95:1 dark.
- `grep -c '"deferred"' scripts/contrast-pairs.json` → `0`
- `grep -rn "data-direction\|graphium-direction" src tests` → no output
- Radix imports in `theme.css` remain 14.

`npm run verify:web` (after `verify:static` 0): 11 failed, 75 passed, 6 skipped.

## Screenshots

`docs/planning/screenshots/006b-baseline/` — 14 PNGs, captured before applying A.

## Decisions raised

none (006-direction A and 006-ia already DECIDED)

## Deviations

- Preflight false-missing Requires backticks (same as 006a).
- Drift vs `8d686c2` is 006a landing.
- `fonts.css` is on the Step 5 Files list but was not edited: A uses Plex Sans and Plex Mono; both faces already live there.

## STOPPED

**Step:** 5 (Check passed; `npm run verify:web` in Commands did not).

**Condition:** CONVENTIONS §2 / §10 — a step that needs a path outside its **Files** list STOPs.
No in-scope retry exists: the three `verify:web` failure classes are owned by later steps or by
no step at all.

**Output (11 failed):**

1. `tests/functional/tokens.spec.ts` — `toolbar-root` `border-radius` expected `8px` (old
   `--app-radius-lg: 0.5rem`), now `4px`. This file is **not** on any 006b step Files list.
2. `tests/visual.spec.ts` — `home light`, `editor light/dark`, `design-system light/dark`
   (pixel diffs from applying A). Snapshots are Step 10 Files.
3. `tests/accessibility.spec.ts` after emptying `CONTRAST_DEFERRED`:
   - `editor-mobile light` — `#ffc53d` on `#fff7c2` (1.45:1). `MobileToolbar.tsx` paints
     active-tool labels with `text-[var(--app-accent-solid)]` on `bg-[var(--app-accent-bg)]`.
     File is Step 7.1.
   - `world` / `world-dialog` light+dark — `#ffffff` on `#ffc53d` (1.57:1).
     `WorldAudioEngine.tsx` "Arm audio" uses `text-white` on `--app-accent-solid`. File is
     Step 9. Confirm-dialog both themes now pass.

**Suggested fix:** Treat `tests/functional/tokens.spec.ts` (`8px` → `4px`) as a Step 5 extra;
leave visual snapshots to Step 10; leave the two component contrast bugs to Step 7.1
(`MobileToolbar` accent-solid-as-text → `--app-accent-text`) and Step 9 (`WorldAudioEngine`
`text-white` → `text-[var(--app-accent-solid-text)]`); accept `verify:web` red until those
steps, with Step 5 Check remaining the gate.

## Handoff

- Branch: `plan/006b-apply-redesign` (PR #286).
- Step 5 token commit: `9eb36b9`. Do not start Step 6 until Kyle unblocks.
- 006b Grounded-at stays `8d686c2`.
- Do not re-add `"deferred"` keys or `data-direction`.
- Do not update visual snapshots before Step 10.
