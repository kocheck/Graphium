# Report: Plan 006b — Apply the redesign

- Branch: plan/006b-close
- Grounded at: 8d686c2
- Started at commit: b946287
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE (this commit)

## Pre-flight

```
preflight: required artefact missing: preflight.sh
preflight: required artefact missing: **Requires**
```

Those two paths are backticks inside the first `**Requires**` parenthetical of
`plans/006-visual-redesign.md`. The real required artefacts exist. The plan file was not edited.

Hand Requires: files-ok; `006-direction.md` and `006-ia.md` both `DECIDED`; `Reviewed-by: Kyle`
count 1.

Drift vs `8d686c2` on 006b paths is not empty — that is 006a landing (directions, playground,
a11y, wcag markers, theme reduced-motion). Recorded; not a second decision file.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                      |
| ---- | ------- | ------------- | ---------- | --------------- | ------------------------------------------ |
| 5    | 9eb36b9 | 0             | 1          | n/a             | Check passed; Commands verify:web failed   |
| 5+   | c0b1bb8 | 0             | n/a        | n/a             | Kyle accepted: tokens.spec 8px→4px extra   |
| 6    | 6642131 | 0             | n/a        | n/a             | duration tokens; elevation-active press    |
| 7.1  | 63c21c9 | 0             | n/a        | n/a             | toolbar; mobile accent-text                |
| 7.2  | 88c3f26 | 0             | n/a        | n/a             | sidebar hairline / inset active            |
| 7.3  | a329672 | 0             | n/a        | n/a             | home screen A tokens                       |
| 7.4  | 89514b0 | 0             | 1          | n/a             | dialogs; merged #287                       |
| 7.5  | 9b8edd0 | 0             | 1          | n/a             | Session Console; merged #288               |
| 8    | 68cf273 | n/a           | n/a        | n/a             | no-op (`true`); no IA restructure          |
| 9    | 8dce2c4 | n/a           | n/a        | n/a             | spec + world tokens                        |
| 9+   | 0ee84d1 | 0             | 1          | n/a             | Kyle: Arm audio primary-on-surface         |
| 10   | 6bb2065 | 0             | 0          | 0               | rebaseline, docs, deploy-web, leftover 127 |

PRs: #286 (steps 5–7.3) `8c31992`; #287 (7.4) `82f45f1`; #288 (7.5) `44889ba`; #289 (8–9) `9187394`.

## Numbers

`npm run contrast` exit 0, no DEFER, no FAIL.

- Light `--app-border-default`: `--slate-8` was 1.86:1; `--slate-9` is 3.22:1. Dark `--slate-8` is 3.01:1.
- Light `--app-error-solid`: white and ink failed on `--red-9` and `--red-10`; `--red-11` + white is 5.21:1.
- Light `--app-success-solid-text`: ink (`--slate-12`) on `--green-9` is 5.19:1.
- Architect accent amber-9 + ink / slate-1: 10.38:1 light / 11.95:1 dark.
- Light `app-accent-solid-text` / `app-accent-solid` in `wcag-audit.md` after `--write`: `#fcfcfd` on `#003362` (12.41:1). That is the World pair — `contrast.mjs` last-wins `[data-view=world]` onto the light map.
- `grep -c '"deferred"' scripts/contrast-pairs.json` → `0`
- `grep -rn "data-direction\|graphium-direction" src tests` → no output
- Radix imports in `theme.css` remain 14.
- Leftover-literal grep (Step 10 Commands): **127** (Q7 permits 151 at `d3d3642`). Not 0. Remaining hits are Canvas / map-generation / tests / comments (`ResourceMonitor.tsx` 23, `DoorShape.tsx` 19, `DungeonGeneratorDialog.tsx` `wallColor = '#ff0000'`, `HomeScreen.tsx` comment `#050505`). Not on any 7.x Files list; Canvas rendering is out of scope.
- `grep -c "placeholder.com" README.md` → `0`
- `test -f public/screenshots/graphium-overview.png` → `present`
- `grep -n "^      - main" .github/workflows/deploy-web.yml` → one line (`6:      - main`)

`npm run verify:web` after 7.4: 77 passed, 9 failed, 6 skipped (known Step 9/10 set).
`npm run verify:web` after 7.5: 76 passed, 9 failed, 1 flaky, 6 skipped (same set).
`npm run verify:web` after Step 9 Kyle fix: 79 passed, 9 failed (visual snapshots only). Rebaselined in Step 10.

Visual rebaseline: `CI=1 npx playwright test tests/visual.spec.ts --project=Web-Chromium --update-snapshots` — 14 passed; rerun without the flag — 14 passed.

`npm run verify` (Step 10 Check): exit 0. `verify:web` 88 passed / 6 skipped; `verify:electron` 7 passed.

## Screenshots

- `docs/planning/screenshots/006b-baseline/` — 14 PNGs, captured before applying A.
- `docs/planning/screenshots/006b-step7.1/` through `006b-step7.5/` — 14 PNGs each.
- `docs/planning/screenshots/006b-final/` — 14 PNGs after direction A + World View gate. Expected: amber Architect chrome, 4px radius, hairline elevation; World overlays at 18px / 2px strokes, no amber on the projector.

Copied `006b-final/editor-dark.png` to `public/screenshots/graphium-overview.png` and `public/screenshots/editor-dark.png`. README lines 61–62 now use `Graphium-show.gif` and `editor-dark.png`.

## Decisions raised

none (006-direction A and 006-ia already DECIDED)

## Deviations

- Preflight false-missing Requires backticks (same as 006a).
- Drift vs `8d686c2` is 006a landing.
- `fonts.css` is on the Step 5 Files list but was not edited: A uses Plex Sans and Plex Mono; both faces already live there.
- `.theme-loading * { transition: none !important }` remains after Step 6 (Commands expected `0`; only the reduced-motion `*` rule was deleted).
- `verify:web` accepted red until 9/10 per Kyle 2026-09-06.
- Shots dirs and this report / README treated as do-required extras.
- Step 9 `[data-view="world"]` solid pair is `#003362` / `#fcfcfd` (light) and `#edeef0` (dark) so `npm run contrast` exits 0.
- Step 9 one retry restyled Minimap caption from `theme.css` (Minimap.tsx is not on the Files list).
- Step 9 Arm audio uses opaque `var(--app-bg-surface)` / `var(--app-text-primary)` after Kyle accepted the fix (the spec `lum()` only parses `rgb()`/`rgba()` and walked past `bg-[var(--app-accent-solid)]` at 1.48:1).
- Leftover-literal count 127, not 0; permitted under Q7 (below 151). Canvas was not edited to chase 0.
- `npm run contrast -- --write` light accent-solid row shows the World pair, not Architect amber (`contrast.mjs` last-wins `[data-view=world]`). `contrast.mjs` is not on Step 10 Files.

## STOPPED (cleared 2026-09-06)

Kyle accepted the suggested fix in chat: treat `tokens.spec.ts` (`8px` → `4px`) as a Step 5 extra;
leave visual snapshots to Step 10; leave the two component contrast bugs to Steps 7.1 and 9;
accept `verify:web` red until those steps. Step 5 Check remains the gate.

**Step:** 5 (Check passed; `npm run verify:web` in Commands did not).

## STOPPED (cleared 2026-09-06, Step 9)

Kyle accepted the suggested Arm audio fix in chat. Applied: opaque `backgroundColor: var(--app-bg-surface)` + `color: var(--app-text-primary)` on the button; dark world `--app-accent-solid-text` is light slate. World solid pair pinned to `#003362` / `#fcfcfd` (light) so `npm run contrast` still exits 0 (`contrast.mjs` last-wins `[data-view=world]` onto the light map).

**Check:** `CI=1 npx playwright test tests/world-legibility.spec.ts --project=Web-Chromium` — 2 passed.

## Handoff

- World View is gated by `tests/world-legibility.spec.ts`. A look on a real second display is Kyle's to do at review.
- 006b Grounded-at stays `8d686c2`. Do not rewrite it.
- `plans/README.md` 006b row stays `IN PROGRESS` until the post-merge run (`plan/006b-post-merge`, commit `plan-006b post-merge: record merge sha`) sets `DONE <merge sha>`.
- Next plan's Grounded at: n/a (last plan of the program).
- Do not re-add `"deferred"` keys or `data-direction`.
- Do not change `--app-*` values in the post-merge commit.
