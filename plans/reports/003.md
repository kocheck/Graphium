# Report: Plan 003 — Build the shared UI primitive layer

- Branch: plan/003-primitive-layer
- Grounded at: 0fae465
- Started at commit: c1837fc
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE

## Pre-flight

`bash scripts/preflight.sh 003` exited 0:

```
preflight: OK: plan 003 (plans/003-build-primitive-layer.md) on plan/003-primitive-layer
```

Drift (`0fae465..origin/main` on in-scope paths) was empty at start. Citation re-check did **not** match every row (see Deviations); continued because the mismatches were leftover from 002 being docs-only on `main` (bridge lived in the patch) plus a prefix-count on `.btn-tool.is-paused`.

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                                                                     |
| ---- | ------- | ------------- | ---------- | --------------- | ----------------------------------------------------------------------------------------- |
| 1    | cf3cd4b | 0             | n/a        | n/a             | ESLint override last in `.eslintrc.cjs`                                                   |
| 2    | 0ad80c8 | 0             | 0          | n/a             | Applied CSS from the patch; excluded playground files so Step 3's 37-count stayed intact  |
| 3    | fbf8694 | 0             | 0          | n/a             | `registry/{index,legacy,buttons,overlays,forms,layout}`; const JSX (not extra components) |
| 4    | 8926bfa | 0             | 0          | n/a             | `tool`/`mode`/`broadcast` + `active` + `state`; extra size `icon-sm`                      |
| 5    | 9d5f796 | 0             | 0          | n/a             | Tranche A; constructor `ResizeObserver`; registry contract test                           |
| 6    | 34923ed | 0             | 0          | n/a             | Tranche B                                                                                 |
| 7    | 221737f | 0             | 0          | n/a             | Tranche C + `ownsEscape`; no `aria-modal` on dropdown (`role="menu"`)                     |
| 8    | dbe5e79 | 0             | 0          | n/a             | vitest `src/components/ui` 0 failed; theme-bridge + portals 8 passed; Button `forwardRef` |
| 9    | fa38077 | 0             | n/a        | n/a             | README 10 headings; UI_RECIPES 5; ADR §14; Prettier wrap retry                            |
| 10   | (this)  | 0             | 0          | 0               | Dry-run `aspect-ratio` discarded; bundle-ok; 14 shots                                     |

## Numbers

| Measure                                          | Command / source                                                                                        | Value                                                             |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `@theme` count                                   | `grep -c '^@theme' src/index.css`                                                                       | 2                                                                 |
| `oklch(` in index.css                            | `grep -c 'oklch(' src/index.css`                                                                        | 0                                                                 |
| `@custom-variant dark`                           | `grep -c '^@custom-variant dark' src/index.css`                                                         | 1                                                                 |
| Primitives (`*.tsx` minus tests)                 | `ls src/components/ui/*.tsx \| grep -vc test`                                                           | 14                                                                |
| `src/components/ui/*.tsx`                        | `ls src/components/ui/*.tsx \| wc -l`                                                                   | 17 (14 primitives + 3 `.test.tsx`)                                |
| Palette-class hits in `src/`                     | plan regex on `*.ts`/`*.tsx`                                                                            | 238 (243 in 39 files at d3d3642; drop from purity fixes in `ui/`) |
| Plan 002 before (web assets)                     | decision-doc baseline                                                                                   | 1188167                                                           |
| Plan 003 today (web assets)                      | `find dist-web/assets -type f \( -name '*.js' -o -name '*.css' \) -print0 \| xargs -0 wc -c \| tail -1` | 1434788                                                           |
| Bundle delta                                     | today − before                                                                                          | 246621                                                            |
| Extrapolated 12-primitive delta                  | decision doc                                                                                            | 269106                                                            |
| Bundle check                                     | `D ≤ 2 × E`                                                                                             | bundle-ok (246621 ≤ 538212)                                       |
| `npx vitest run src/components/ui` (Step 8)      | failed count                                                                                            | 0                                                                 |
| theme-bridge + portals (Step 8 Check)            | Web-Chromium                                                                                            | 8 passed                                                          |
| `npm run verify` (Step 10)                       | exit code                                                                                               | 0                                                                 |
| README dry-run `verify:static`                   | after `aspect-ratio` add + register + a11y case                                                         | 0                                                                 |
| `git status --porcelain \| grep -c aspect-ratio` | after discard                                                                                           | 0                                                                 |

## Screenshots

`SHOTS_OUT=docs/planning/screenshots/003-final npm run shots` — 14 passed. Kyle: review the two design-system frames (viewport, not full-page; `tool`/`mode`/`broadcast` rows sit below the first screen). Other surfaces should match plan 001 (`visual.spec.ts` passed inside `verify:web`).

- `docs/planning/screenshots/003-final/design-system-light.png` — playground, light; new `ui-*` examples registered (53 components shown)
- `docs/planning/screenshots/003-final/design-system-dark.png` — playground, dark; same registry, theme via `[data-theme]`
- `docs/planning/screenshots/003-final/{home,editor,editor-mobile,confirm-dialog,world,world-dialog}-{light,dark}.png` — unchanged chrome; included because `shots` captures every surface

## Decisions raised

none

## Deviations

1. **Citation table stale at start.** `@theme` expected 2 / actual 1; `@custom-variant dark` expected 1 / actual 0; `.btn-tool.is-paused` expected 1 / actual 2 (prefix of the hover rule). 002 landed docs-only; the bridge lived in `docs/planning/shadcn-spike.patch`. Did not STOP; applied CSS from the patch in Step 2.
2. **Patch apply.** Plan assumed the bridge was already on `main`, so its apply excluded `src/index.css`. Actual: applied `src/index.css` and excluded playground-registry/types so Step 3's 37-example count stayed intact. Also excluded `.eslintrc.cjs`, `src/App.tsx`, spike scaffold, `spike-alias.test.ts`.
3. **Dialog Button import** `@/components/ui/button` → `./button` (`no-restricted-imports` blocks `@/components/*`). Same for Sheet.
4. **ResizeObserver** constructor stub in `src/test/setup.ts` (Step 5; not a Step 2 file).
5. **Playground registry uses const JSX**, not extra function components (`react-refresh/only-export-components` is not off there). Bridge probe is `bridgeProbe`, not `function BridgeProbe`.
6. **Destructive playground button omitted** (decision §10.4). CVA still has the variant.
7. **Select playground** `aria-label="Grid type"` (axe `button-name`).
8. **No `aria-modal` on DropdownMenuContent** — `role="menu"` fails axe `aria-allowed-attr`.
9. **Slider** `bg-white` → `bg-background` (purity); nested ternary un-nested (`no-nested-ternary`); required `thumbLabel`.
10. **Button `icon-sm`** so generated dialog/sheet close buttons type-check.
11. **Button `forwardRef`** so Radix `asChild` can restore focus (dropdown portal spec). Step 8 Files did not list `button.tsx` / `dropdown-menu.tsx`; both were required for the Check and committed with Step 8.
12. **`printf 'n\n' \| npx shadcn@4.21.0 add sheet -y`** — CLI prompted to overwrite `button.tsx`.
13. **README dry-run.** CLI `aspect-ratio` omitted the React import. Added `import type * as React from 'react'` so `tsc` and `consistent-type-imports` pass. Documented that (and “answer `n` if add asks to overwrite `button.tsx`”) in `## Add a primitive`. `verify:static` then 0. Discarded; porcelain was only the README.
14. **Did not raise `003-icon-library` / `003-destructive-contrast`.** CONVENTIONS §9 already allows lucide-react inside `src/components/ui/`; destructive contrast is recorded in the 002 decision doc, not a new Kyle question.
15. **TOC line for ADR 14** uses different wording so `grep -c 'Adopt shadcn/ui primitives'` stays 1. Summary Table not rewritten.

## STOPPED

n/a

## Handoff

- Next plan's Grounded at: ‹003 merge SHA, filled after merge›
- Things the next executor should know:
  - Post-merge run: branch `plan/003-post-merge`, one commit `plan-003 post-merge: record merge sha` — set `plans/README.md` row 003 to `DONE <merge sha>`; write that SHA into `plans/004-migrate-screens-to-primitives.md` **Grounded at** and the first **Grounded at** of `plans/006-visual-redesign.md` (leave **Grounded at (006b)** for plan 005).
  - Do not start 004 until that post-merge SHA is written.
  - 006a still STOPs if `docs/planning/ui-redesign-brief.md` §11 is blank (mood words, references, direction-table edits).
  - New primitives: `npx shadcn@4.21.0 add <name> -y` then `src/components/ui/README.md` § Add a primitive. If the CLI asks to overwrite `button.tsx`, answer `n`.
  - Overlay `Content` must keep `ownsEscape` (default true → `data-esc-owns="true"`). Do not put `aria-modal` on `role="menu"`.
  - Sibling imports inside `src/components/ui/` must be `./button`, not `@/components/ui/button`.
  - Do not add `sonner` or `command`. Do not migrate existing screens (004).
  - Merge method: merge commit, never squash.
