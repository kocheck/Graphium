# Report: Plan 004 PR 5/6 — Toolbars

- Branch: plan/004-pr5-toolbars
- Grounded at: 5f80d37 (PR 4)
- Started at commit: 28cbf73
- Executor: Cursor Grok 4.6 (cloud agent)
- Result: DONE (PR 5/6; waiting on merge)

## Pre-flight

`bash scripts/preflight.sh 004` exited 0 on `plan/004-pr5-toolbars`.

Drift vs `5f80d37` on in-scope `src/` `tests/` `scripts/` `.eslintrc.cjs` `docs/guides/UI_RECIPES.md` was empty. Used the Status line **Grounded at (PR 4)** by hand (the plan's `Grounded at\*\*:` grep does not match `(PR k)` lines).

## Steps

| Step | Commit  | verify:static | verify:web | verify:electron | Notes                                                      |
| ---- | ------- | ------------- | ---------- | --------------- | ---------------------------------------------------------- |
| 10   | 79d176e | 0             | 0          | n/a             | Toolbar extract; `size="tool"` `px-3! py-1!`; BASELINE 234 |
| 11   | (this)  | 0             | 0          | 0               | MobileToolbar; BASELINE 233                                |

## Numbers

### Card rows (before → after)

```
| `src/components/Toolbar.tsx` | new; BEFORE_IDS=8 BEFORE_LABELS=8 | after App.tsx toolbar- ids=0 Toolbar.tsx ids=8 aria-label=8 btn/divider=0 |
| `src/components/MobileToolbar.tsx` | before lines=328 palette=1 inline=14 | after lines=301 ui-imports=1 role=0 aria-modal=0 Escape=0 esc-owns=0 test=no palette=0 inline=1 btn=0 legacy=0 | data-testid="toolbar-mobile-root" |
```

### Ratchet

| After step | BASELINE |
| ---------- | -------- |
| PR 4       | 234      |
| 10         | 234      |
| 11         | 233      |

### Toolbar counts

- `BEFORE_IDS=8` `BEFORE_LABELS=8`
- After: `src/App.tsx:0` `src/components/Toolbar.tsx:8` ids; `8` aria-labels; `btn btn-|toolbar-divider` is `0` in both files

### MobileToolbar counts

- `min-h-[56px]`: 10
- `style={{`: 1 (colour swatch)

## Screenshots

- `docs/planning/screenshots/004-step10/` — 14 files; expected differences none (toolbar matches baseline)
- `docs/planning/screenshots/004-step11/` — 14 files; expected differences none on `editor-mobile` (more-menu closed)

## Decisions raised

none

## Deviations

1. **Drift grep vs `(PR k)`**. Used `5f80d37` from the Status line (empty vs `origin/main` on in-scope paths).
2. **`Layout components` heading**. `grep -n 'Layout components' src/components/README.md` was 0. Added a `**Layout components**` bullet naming `Toolbar.tsx`.
3. **Import order**. Plan said `import Toolbar` after `Toast`. `import/order` wants `TokenInspector` before `Toolbar`. Followed lint.
4. **Exported Toolbar types**. `import/no-unused-modules` warned on the plan-required type exports. App.tsx now uses `ToolbarTool`, `DoorOrientation`, and `MeasurementMode`. `ToolbarProps` keeps an unused-modules disable for plan 005.
5. **`size="tool"` padding**. Leftover `p-2` on tool buttons became 38×38 under Tailwind merge. Fixed `src/components/ui/button.tsx` to `px-3! py-1!` so the 46×30 target matches unlayered `.btn`.
6. **mobile-smoke without `CI=1`**. Agentation's DEV feedback toolbar intercepted the More button. The same specs pass under `CI=1` (preview, no Agentation). `verify:web` is the gate.

## STOPPED

n/a

## Handoff

- Next plan's Grounded at: (filled after PR 5 merge)
- Next run: branch `plan/004-pr5-post-merge` from `origin/main`, then `plan/004-pr6-sweep` (steps 12–14). Drift check uses `| tail -1` so it picks **Grounded at (PR 5)**.
- 006a is still blocked on `docs/planning/ui-redesign-brief.md` §11.
- Merge method: merge commit, never squash.
