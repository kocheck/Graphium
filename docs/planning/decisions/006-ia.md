# Decision 006-ia: For each of seven IA questions, keep, adjust or restructure?

Status: PENDING

## Question

Separately from appearance, is what-lives-where right? Answer each numbered question with
exactly one of `keep | adjust | restructure`. "Keep" is a legitimate answer and is recorded as
one. Direction chosen in 006-direction: A. Audit conclusion:
identity gap (`grep "^Conclusion" docs/planning/ui-redesign-audit.md`).

### Q1 Toolbar

Current: floating bar, `fixed bottom-4 left-1/2` (`grep -rn "toolbar fixed bottom-4" src/App.tsx src/components/Toolbar.tsx`,
`src/components/Toolbar.tsx:69`). Tool options (colour, measurement mode, door orientation) are inline.

### Q2 Sidebar

Current: three components — `src/components/Sidebar.tsx` (487 lines), `QuickTokenSidebar.tsx`
(168), `MobileSidebarDrawer.tsx` (59) (`wc -l`).

### Q3 Settings

Current: two surfaces — `src/components/MapSettingsSheet.tsx` (447 lines) and
`src/components/SessionConsole/SessionConsoleSettingsSheet.tsx` (49 lines) with
`sessionConsoleSettingsSections.tsx` (272 lines; 4 exported field groups:
`grep -c '^export function SessionConsole' src/components/SessionConsole/sessionConsoleSettingsSections.tsx`,
4, same as at d3d3642).

### Q4 Home screen

Current: `src/components/HomeScreen.tsx`, 1802 lines; presents recent campaigns, templates, new
campaign and settings on one screen (`grep -c "data-testid=\"home-" src/components/HomeScreen.tsx`
→ 0 test ids).

### Q5 Session Console

Current: `src/components/SessionConsole/` (24 files, `ls | wc -l`), designed in
`docs/planning/session-console-design.md`; opens inside the sidebar.

### Q6 World View

Current: canvas only at `?type=world`; overlay text elements: 0
(`[data-testid^="world-"]` count from `docs/planning/ui-redesign-audit.json`, surface `world`).
The brief §8 allows a `[data-view="world"]` token set; answer whether it gets one.

### Q7 Hardcoded colours in files plan 004 does not touch

Current: 230 palette classes and 141 literal colours (commands in the audit's Counts section).
Options here are `keep | fix-in-006b | defer`.

## Options

For Q1–Q6: 1. keep — no behaviour change, 006b Step 7 restyles in place. 2. adjust — same
elements, new placement or grouping; additive `data-testid`s only. 3. restructure — flows change;
006b Step 8 owns it with its own E2E additions. For Q7: 1. keep — the palette swap does not reach
those files. 2. fix-in-006b — 006b Step 7 adds a per-file sub-step. 3. defer — a follow-up plan.

## Recommendation

Q1 keep — audit editor Depth is 8px radius and a 25px blur shadow; 006b restyles the floating bar
in place.
Q2 keep — audit conclusion is identity gap (tech-blue accent), not a three-file sidebar split
(487 / 168 / 59).
Q3 keep — two surfaces already split map vs session (447 lines; 4 exported SessionConsole groups);
audit Colour is `#0070c1` on every surface.
Q4 keep — audit home Colour / Density (`#0070c1`, 4px padding) is appearance; one landing screen,
0 `home-` test ids.
Q5 keep — 24 files already designed in `docs/planning/session-console-design.md` and open inside
the sidebar; audit world Motion cites WorldStage (1 transition), not console placement.
Q6 adjust — audit JSON surface `world` has 0 `[data-testid^="world-"]` hits; brief §8 still allows
a `[data-view="world"]` token set so direction A's amber does not bleed onto the projection
(audit world Colour `#0070c1`).
Q7 fix-in-006b — Counts list 230 palette classes and 141 literals; those leftovers keep tech-blue
`#0070c1` (audit Colour rows) after the direction A swap unless 006b Step 7 reaches those files.

## Kyle's answer
